# Releasing

The release procedure (version bump → build → commit → tag → push, and what the CI workflows do)
lives in the `release` skill at [`.claude/skills/release/SKILL.md`](.claude/skills/release/SKILL.md),
so there's a single source of truth for the steps.
