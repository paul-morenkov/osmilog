name: WASM build automation
run-name: ${{ github.actor }} is building to WASM
on: [push]
jobs:
  Build-WASM:
    runs-on: ubuntu-latest
    steps:
      - run: cargo build --release --target wasm32-unknown-unknown
      - run: cp ./target/
